# air configuration — https://posit-dev.github.io/air/
# `renv/`, `.git/`, revdep/, RcppExports.R, etc. are skipped automatically via
# air's built-in `default-exclude`, so no explicit `exclude` list is needed.
[format]
line-width = 80
